declare enum RepeatEnum {
    never = 0,
    run_once = 1,
    run_every_hour = 2,
    run_every_day = 3,
    run_every_week = 4,
    run_every_month = 5,
    run_every_year = 6,
}
export = RepeatEnum;
